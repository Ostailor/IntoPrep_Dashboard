{
  "name": "intoprep-dashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "ci": "npm run lint && npm run typecheck && npm run test && npm run build",
    "desktop:prepare": "node scripts/prepare-desktop-config.mjs",
    "desktop:dev": "npm run desktop:prepare -- --mode=dev && tauri dev --config src-tauri/tauri.desktop.conf.json",
    "desktop:build": "npm run desktop:prepare && tauri build --config src-tauri/tauri.desktop.conf.json",
    "desktop:icon": "tauri icon public/pwa-icon.svg",
    "db:push": "npx supabase db push --include-all --yes",
    "db:reset": "npx supabase db reset",
    "db:new": "npx supabase migration new"
  },
  "dependencies": {
    "@supabase/ssr": "^0.7.0",
    "@supabase/supabase-js": "^2.57.4",
    "clsx": "^2.1.1",
    "lucide-react": "^0.518.0",
    "next": "16.1.6",
    "react": "19.2.3",
    "react-dom": "19.2.3"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@tauri-apps/cli": "^2.10.1",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^4.0.7"
  }
}
